'use strict';
(function() {
//console.log("freesurfer ui.js");

var service = angular.module('sca-service-freesurfer', [ ]);
service.directive('scaStepUpload', ['appconf', 'serverconf', 'toaster', 'Upload', 'resources',
function(appconf, serverconf, toaster, Upload, resources) {
}]);

//end of IIFE (immediately-invoked function expression)
})();
